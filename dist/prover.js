"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var pyt_merkle_sum_tree_1 = require("pyt-merkle-sum-tree");
var snarkjs_1 = require("snarkjs");
var buildCircomInput_1 = require("./buildCircomInput");
/**
 * Prover is a class that contains the core methods to let CEXs provide credible Proof Of Solvency to its users
 * while maintaining secrecy over their Business Information thanks to zkSNARKs.
 * The proof doesn't reveal any information such as the total balances of each users, the number of users and total amount of liabilities of the exchange.
 */
var Prover = /** @class */ (function () {
    /**
     * Initializes the prover
     * @param tree The Merkle Sum Tree generated by the prover.
     * @param assetsSum The total assets owned by the exchange.
     * @param proverArtifacts The artifacts of the prover.
     */
    function Prover(tree, assetsSum, proverArtifacts) {
        this._tree = tree;
        this._assetsSum = assetsSum;
        this._proverArtifacts = proverArtifacts;
        // freeze the object to prevent any modification
        Object.freeze(this);
    }
    Object.defineProperty(Prover.prototype, "tree", {
        get: function () {
            return this._tree;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Prover.prototype, "assetsSum", {
        get: function () {
            return this._assetsSum;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Generate a proof of solvency for a specific user.
     * @param userIndex The index of the user in the csv file.
     * @returns A proof of solvency.
     */
    Prover.prototype.generateProofForUser = function (userIndex) {
        return __awaiter(this, void 0, void 0, function () {
            var circomInput, _a, proof, publicSignals, entry, fullProof;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        circomInput = (0, buildCircomInput_1.default)(this._tree, userIndex, this._assetsSum);
                        return [4 /*yield*/, snarkjs_1.groth16.fullProve(circomInput, this._proverArtifacts.wasmFilePath, this._proverArtifacts.zkeyFilePath)];
                    case 1:
                        _a = _b.sent(), proof = _a.proof, publicSignals = _a.publicSignals;
                        entry = new pyt_merkle_sum_tree_1.Entry(circomInput.username, circomInput.balance);
                        fullProof = {
                            entry: entry,
                            rootHash: publicSignals[1],
                            assetsSum: publicSignals[2],
                            proof: proof,
                        };
                        return [2 /*return*/, fullProof];
                }
            });
        });
    };
    return Prover;
}());
exports.default = Prover;
